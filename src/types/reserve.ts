export type ReserveRecord = {
  id: number
  params: string
  execute_at: string
  status: string
  alarm_namespace: string
  alarm_object_id: string
  alarm_scheduled_at: string
  created_at: string
}
